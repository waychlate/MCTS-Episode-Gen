# MCTS-Episode-Gen

A tool to generate highway-env driving episodes using the MCTS algorithim.

## What do the scripts in tools do?
- image_preview.py: preview one of the training episodes using Pillow to ensure they look correct
- image_processing.py: downscale and crop the images
- sort_dataset.py: split the original pool of data into two, train and test 